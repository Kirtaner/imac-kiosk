/* Routes */

Router.route('/', {
  template: 'kiosk'
});

Router.route('/demo', {
  template: 'demo'
});

Router.route('/customer', {
  template: 'customerForm'
});

Router.route('/list', {
  template: 'customerList'
});

Router.route('/build', {
  template: 'build'
});
